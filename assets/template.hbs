<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Docs</title>
  <link rel="stylesheet" href="{{relativeAssetsPath}}/node_modules/bootstrap/dist/css/bootstrap.css">
  <link rel="stylesheet" href="{{relativeAssetsPath}}/node_modules/highlight.js/styles/tomorrow-night.css">
  <script src="{{relativeAssetsPath}}/node_modules/jquery/dist/jquery.js"></script>
  <script src="{{relativeAssetsPath}}/node_modules/bootstrap/dist/js/bootstrap.js"></script>
</head>
<body>
  <table class="table">
    <tbody>
      {{#each comments}}
        <tr>
          <td class="col-md-6" style="padding-left: 20px;">
            <h{{importance}}>
              <code>{{title}}</code>
              {{#unless isModule}}
                {{#if api}}<small>&lt;{{api}}&gt;</small>{{/if}}
              {{/unless}}
            </h{{importance}}>

            {{{description.full}}}

            {{#if params}}
              <table class="table table-bordered table-striped">
                <thead>
                  <tr>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Types</th>
                  </tr>
                </thead>
                <tbody>
                  {{#each params}}
                    <tr>
                      <td>{{name}}</td>
                      <td>{{description}}</td>
                      <td>{{types}}</td>
                    </tr>
                  {{/each}}
                </tbody>
              </table>
            {{/if}}

            {{#if return}}
              <p><strong>Returns:</strong> ({{return.types}}) {{return.description}}</p>
            {{/if}}

            <p><a data-toggle="collapse" href="#raw-{{id}}">See raw doc output &rarr;</a></p>

            <div class="collapse" id="raw-{{id}}">
              <pre style="max-height: 300px; max-width: 600px; overflow: auto;">{{json .}}</pre>
            </div>
          </td>
          <td class="col-md-6" style="background: #333; color: #eee; border-color: #222">
            <pre style="background: transparent; color: inherit; border: none">{{{code}}}</pre>
          </div>
        </tr>
      {{/each}}
    </tbody>
  </table>
</body>
</html>
